(function($){
    'use strict';

    // Placeholder for potential future enhancements.
    $(document).on('ready', function(){
        if ( window.wrCampaignBarFullData && window.wrCampaignBarFullData.debug ) {
            // console debug hook
        }
    });

})(jQuery);
